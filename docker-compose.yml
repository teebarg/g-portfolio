services:
  portfolio:
    ports:
      - "8080:8000"
      - "9001:9001"
    build:
      context: ./
    command: npm run develop
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    env_file:
      - .env
